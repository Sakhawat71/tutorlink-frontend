import React from 'react';
import { HashLoader } from 'react-spinners';

const Tutuorloading = () => {
    return (
        <div className="flex items-center justify-center h-screen">
            <HashLoader />
        </div>
    );
};

export default Tutuorloading;